% ──────────────────────────────────────────────────────────────────────────────
%  Capitolo 3 – Data Understanding
% ──────────────────────────────────────────────────────────────────────────────
\chapter{Data Understanding}
\label{ch:data_understanding}

Il presente capitolo è dedicato all'esplorazione, comprensione e preparazione dei dati necessari per l'addestramento dei modelli di Intelligenza Artificiale previsti dal progetto SINTON-IA. Per ciascun modello vengono dettagliate le fonti di acquisizione, le motivazioni della scelta e la struttura tecnica dei dataset identificati.

% ── 3.1 ──────────────────────────────────────────────────────────────────────
\section{Dataset per il Modello di Red Flag Detection}
\label{sec:data_red_flag}

\subsection{Acquisizione Dati}
\label{subsec:acquisizione_red_flag}

Come parzialmente anticipato nella fase di Business Understanding, l'acquisizione di dati clinici legati alla salute mentale è fortemente ostacolata da stringenti normative sulla privacy. Lo sviluppo di un modello di NLP specializzato nell'identificazione del rischio suicidario necessita di dati testuali massivi in cui gli autori esprimano i propri stati d'animo in contesti non strutturati (es. diari, blog, forum), corredati da etichette di rischio affidabili.

Nel corso della ricerca sulle fonti dati, sono stati individuati due dataset primari, ciascuno con caratteristiche e livelli di accessibilità differenti.

Il primo è \textbf{UMD Reddit Suicidality Dataset} \cite{umd_reddit_suicidality}, realizzato dall'Università del Maryland. Si tratta di un dataset accademico di altissimo livello qualitativo: i post estrapolati da Reddit sono stati annotati sia tramite \textit{crowdsourcing} sia da esperti clinici utilizzando la scala standard \textbf{C-SSRS} (Columbia-Suicide Severity Rating Scale). Rappresenta il \textit{gold standard} per questo tipo di task predittivi.
Tuttavia, a causa della delicatezza dei contenuti, gli autori ne hanno reso pubblica solo una versione fortemente ridotta e anonimizzata contenente circa 500 istanze, un numero totalmente insufficiente per addestrare da zero o affinare (fine-tuning) un modello di NLP moderno (es. \textit{Transformers}). L'accesso al dataset completo richiede una formale approvazione da parte di un Comitato Etico Istituzionale (IRB) e l'implementazione di rigidi protocolli di sicurezza per la conservazione dei dati protetti.

Il secondo dataset individuato è \textbf{Suicide Watch Dataset} \cite{suicide_watch_dataset}, disponibile pubblicamente sulla piattaforma Kaggle. Sebbene non raggiunga il livello di validazione clinica certificata tramite C-SSRS del dataset UMD, esso si basa anch'esso sull'estrapolazione massiva di testi dalla piattaforma Reddit (tipicamente dai subreddit \textit{r/SuicideWatch} e altri forum dedicati al supporto psicologico o temi generali). 

Alla luce di questi vincoli, è stata definita la seguente strategia: \textbf{il dataset "Suicide Watch" di Kaggle verrà impiegato per la fase prototipale del progetto}. La sua natura pubblica e la mole di dati disponibile ne consentono l'impiego immediato per validare l'architettura del modello, sintonizzare gli iperparametri e dimostrare la fattibilità tecnica del sistema \textit{Red Flag}. Come sviluppo futuro in caso di adozione reale del sistema, il team richiederà l'autorizzazione etica per accedere al dataset UMD (o equivalenti clinici), al fine di specializzare e ri-calibrare il modello con dati certificati in maniera professionale.

\subsection{Descrizione dei Dati}
\label{subsec:descrizione_red_flag}

Il dataset pubblico \textit{Suicide Watch} scelto per l'addestramento prototipale si presenta in formato puramente testuale e tabellare (file CSV). 

La sua struttura è volutamente essenziale per favorire task di classificazione binaria nell'ambito del \textit{Natural Language Processing}. Di seguito le caratteristiche principali:

\begin{itemize}
    \item \textbf{Volume dei dati:} Il dataset comprende oltre 230.000 record complessivi, un volume più che adeguato per eseguire il \textit{fine-tuning} di modelli pre-addestrati come BERT o varianti più leggere (DistilBERT).
    \item \textbf{Bilanciamento delle classi:} Le istanze sono suddivise in modo pressoché paritetico ($\sim$50/50) tra le due classi bersaglio, mitigando il problema dello sbilanciamento tipico dei dati clinici e facilitando l'apprendimento delle \textit{feature} testuali discriminanti da parte della rete neurale.
\end{itemize}

Ogni record (riga) del dataset è descritto da due colonne fondamentali:
\begin{enumerate}
    \item \textbf{\texttt{text}} (Stringa): contiene il contenuto testuale non strutturato scritto dall'utente. Questi testi rispecchiano fedelmente le dinamiche dello \textit{user-generated content} (UGC) online: variano notevolmente in lunghezza, presentano spesso un registro informale, errori ortografici, e descrivono pensieri, vicende personali o espressioni dirette di dolore emotivo. Questa eterogeneità stilistica li rende una "palestra" eccellente per simulare l'imprevedibilità formale dei testi che i pazienti inseriranno nei diari di SINTONIA.
    \item \textbf{\texttt{class}} (Label): è la variabile target del modello, che si presenta sotto forma di etichetta testuale per distinguere:
    \begin{itemize}
        \item \texttt{suicide}: indica che il testo contiene espressioni di ideazione suicidaria, intenti autolesionistici gravi o disperazione acuta tale da far presumere un pericolo imminente.
        \item \texttt{non-suicide}: indica che il testo esplora argomenti di tipo generale o, pur trattando esperienze di tristezza, ansia o difficoltà, non manifesta i pattern critici ed emergenti associabili al rischio estremo.
    \end{itemize}
\end{enumerate}

\subsection{Esplorazione e Qualità dei Dati}
\label{subsec:esplorazione_red_flag}

Al fine di validare l'idoneità del dataset e pianificare le opportune strategie di \textit{Data Preparation}, è stata condotta un'analisi esplorativa (Exploratory Data Analysis, EDA) documentata all'interno dei Jupyter Notebook associati al repository del modello. 

\textbf{Bilanciamento del Dataset:} L'analisi ha confermato la totale assenza di valori nulli (\textit{missing values}) strutturali nelle colonne principali e un equilibrio perfetto delle etichette: su un totale esatto di 232.074 record, figurano 116.037 istanze per la classe \texttt{suicide} e 116.037 istanze per la classe \texttt{non-suicide} (Figura~\ref{fig:rf_class_balance}). Questa simmetria garantisce che la rete non svilupperà intrinsecamente un \textit{bias} verso la classe maggioritaria.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.65\textwidth]{rf_class_balance.png}
    \caption{Bilanciamento perfetto delle classi nel dataset "Suicide Watch".}
    \label{fig:rf_class_balance}
\end{figure}

\textbf{Distribuzione delle Lunghezze e Limiti Architetturali (2000 Caratteri):}
Un aspetto essenziale emerso durante il \textit{Business Understanding} è che la piattaforma SINTONIA imporrà un limite tassativo di \textbf{2.000 caratteri} per la compilazione dei diari da parte dei pazienti. Pertanto, analizzare la lunghezza dei testi del dataset non in "numero di parole" ma in "numero di caratteri" è cruciale per prevenire un \textit{bias} strutturale. 

Lo script di analisi ha rilevato una lunghezza testuale media di circa 690 caratteri per post, con la maggior parte della distribuzione (mediana) concentrata intorno ai 315 caratteri. Tuttavia, focalizzandosi sul limite di tolleranza pattuito, l'esplorazione (Figura~\ref{fig:rf_char_len_dist}) ha evidenziato che \textbf{oltre 17.000 record (il 7,3\% del totale) superano la soglia dei 2.000 caratteri}, con picchi estremi superiori ai 40.000 caratteri.
Addestrare un modello su documenti lunghissimi per poi impiegarlo in inferenza su frammenti corti (limitati dal frontend) causerebbe un crollo drammatico dell'accuratezza in fase di produzione.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{rf_char_len_dist.png}
    \caption{Distribuzione della lunghezza dei testi in caratteri. La linea rossa tratteggiata indica il limite di 2.000 caratteri previsto per l'app SINTONIA.}
    \label{fig:rf_char_len_dist}
\end{figure}

\textbf{Analisi del Rumore Testuale (Noise):} 
L'estrapolazione di dati da una piattaforma web non regolamentata come Reddit comporta l'introduzione di notevole "rumore" sintattico e semantico. L'analisi quantitativa tramite espressioni regolari ha messo in luce:
\begin{itemize}
    \item L'inclusione di \textbf{URL / Link esterni} in oltre 7.100 post (circa il 3,1\% del dataset).
    \item Una \textbf{formattazione complessa o dispersiva} (sequenze multiple di \textit{newline} o ritorni a capo) presente nel 36,7\% dei record, tipica dei forum online ma inverosimile per la textarea di un diario clinico privato.
    \item Un'ingente presenza di \textbf{Stop Words} (le parole grammaticali di contorno, es. pronomi, articoli, preposizioni), che saturano mediamente il 45,4\% di ciascun documento senza apportare semantica discriminante ai fini predittivi del rischio e minacciando l'efficienza dei layer di \textit{Attention} dei Transformers impiegati.
\end{itemize}

Le evidenze raccolte impongono, dunque, una fase di manipolazione preliminare severa. La \textit{Data Preparation} successiva dovrà obbligatoriamente implementare filtri per eliminare il rumore (URL e formattazioni errate), decurtare le stop-words e, soprattutto, imporre una troncatura e ri-scansione delle sequenze testuali superiori alla soglia architetturale di 2.000 caratteri imposta dal frontend dell'applicativo.


% ── 3.2 ──────────────────────────────────────────────────────────────────────
\section{Dati per il Modello di Depression Prediction}
\label{sec:data_depression}

Il modello di \textit{Depression Prediction} di SINTON-IA ha l'obiettivo di stimare il punteggio di gravità depressiva di ciascun utente --- misurato tramite il questionario clinico standardizzato \textbf{PHQ-9} \cite{kroenke2001phq9} --- a partire dalle serie temporali giornaliere dell'umore registrate dall'applicazione nell'arco di 14 giorni. La scala PHQ-9, validata da Manea et al. \cite{manea2012phq9}, produce un punteggio complessivo compreso tra 0 e 27 che consente di classificare la severità depressiva in cinque fasce cliniche: minima (0--4), lieve (5--9), moderata (10--14), moderatamente severa (15--19) e severa (20--27).

\subsection{Il Problema: Disponibilità di Dati Clinici Longitudinali}
\label{subsec:problema_dati_depression}

La costruzione di un modello predittivo per la depressione richiede dataset longitudinali in cui, per ciascun soggetto, siano disponibili sia le registrazioni giornaliere dell'umore sia una valutazione clinica riassuntiva (il punteggio PHQ-9 finale). Dataset di questo tipo sono estremamente rari nella letteratura aperta per tre ragioni principali: (i) le normative sulla privacy (\textit{GDPR}, \textit{HIPAA}) limitano la condivisione di dati sanitari sensibili; (ii) le campagne di raccolta EMA (\textit{Ecological Momentary Assessment}) sono costose e soggette ad alto tasso di abbandono \cite{aanhetrot2012ema}; (iii) i pochi dataset esistenti presentano campioni ridotti e contesti altamente specifici, inadatti a una generalizzazione diretta.

Per queste ragioni, si è adottata una strategia in due fasi: (a) l'analisi di due dataset di riferimento pubblici per estrarne \textit{pattern statistici} utili, e (b) la progettazione di un \textbf{generatore di dati sintetici avanzato}, guidato primariamente dalla letteratura scientifica peer-reviewed, in grado di produrre un corpus di addestramento sufficientemente ampio e clinicamente plausibile.

\subsection{Analisi dei Dataset di Riferimento}
\label{subsec:analisi_dataset_depression}

Sono stati individuati due dataset pubblici le cui caratteristiche, pur non essendo direttamente compatibili con il task di SINTON-IA, offrono informazioni statistiche preziose per calibrare il generatore sintetico. I dati grezzi sono stati organizzati nella cartella \texttt{data/raw/} del repository.

\subsubsection{StudentLife Dataset}

Il primo dataset è lo \textbf{StudentLife Dataset} \cite{studentlife_dataset}, prodotto dalla Dartmouth University. Lo studio ha raccolto dati multi-modali --- tra cui questionari EMA sull'umore (felicità, stress, fatica) e il questionario PHQ-9 somministrato prima e dopo il periodo di monitoraggio --- per un campione di circa 48 studenti universitari americani.

I file utilizzati comprendono il CSV con i punteggi PHQ-9 pre/post (\texttt{studentlife\_\allowbreak{}phq9\_\allowbreak{}scores.csv}) e le cartelle JSON contenenti le risposte giornaliere sullo stato d'animo (\texttt{studentlife\_\allowbreak{}mood\_\allowbreak{}detailed/}, \texttt{studentlife\_\allowbreak{}mood\_\allowbreak{}current/}).

\textbf{Limiti:} il campione è numericamente ridotto e rappresenta un contesto estremamente specifico (studenti universitari americani sottoposti a stress accademico), il che ne preclude l'impiego diretto come dataset di addestramento. Tuttavia, le correlazioni osservate tra variazioni dell'umore e punteggi PHQ-9 forniscono informazioni utili sulla \textbf{variabilità naturale dell'umore umano} e sulla \textbf{persistenza degli stati emotivi} (inerzia emotiva).

\subsubsection{14-Day Ambulatory Assessment}

Il secondo dataset è il \textbf{14-Day Ambulatory Assessment} \cite{kaggle_14day_depression}, contenuto nel file \texttt{14day\_\allowbreak{}ema\_\allowbreak{}depression\_\allowbreak{}log.csv}. Esso documenta oltre 300 soggetti monitorati quotidianamente per 14 giorni, a ciascuno dei quali è stato somministrato il questionario PHQ-9 in ogni sessione giornaliera, insieme a un punteggio complessivo di felicità (\textit{happiness score}).

\textbf{Limiti:} la struttura è sostanzialmente inversa rispetto al task di SINTON-IA. In questo dataset, il PHQ-9 viene compilato quotidianamente (non una sola volta al termine del periodo), mentre il punteggio di felicità rappresenta l'esito sintetico. Nel nostro caso, invece, l'utente registra giornalmente il suo umore nell'app e il modello deve stimare il PHQ-9 complessivo dal pattern temporale osservato. Per tale ragione, anche da questo dataset si estraggono esclusivamente le \textbf{matrici di transizione emotiva} (quanto è probabile passare da uno stato d'animo negativo a uno neutro o positivo il giorno successivo) e i \textbf{tassi di mancata risposta} per fascia di gravità.

\subsubsection{Mappatura nello Spazio Valence-Arousal}

Poiché le scale emotive dei due dataset non coincidono con la griglia $10 \times 10$ di SINTONIA (10 stati d'animo $\times$ 10 livelli di intensità), è stata effettuata una mappatura unificante tramite il \textbf{Modello Circomplesso dell'Affetto} introdotto da Russell \cite{russell1980circumplex} e successivamente validato da Posner et al. \cite{posner2005circumplex}. In questo spazio bidimensionale, ogni emozione è descritta da due coordinate ortogonali:
\begin{itemize}
    \item \textbf{Valence} (valenza emotiva): da $-1$ (molto negativo) a $+1$ (molto positivo);
    \item \textbf{Arousal} (attivazione fisiologica): da $-1$ (passivo, letargico) a $+1$ (attivo, energetico).
\end{itemize}

I 10 stati d'animo dell'app SINTONIA sono stati posizionati come coordinate fisse in questo spazio (Figura~\ref{fig:va_map}), consentendo di proiettare qualsiasi risposta dei dataset originali nella medesima rappresentazione normalizzata.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.70\textwidth]{dp_valence_arousal_map.png}
    \caption{Proiezione dei 10 stati d'animo SINTONIA nello spazio Valence-Arousal del Modello Circomplesso di Russell \cite{russell1980circumplex}.}
    \label{fig:va_map}
\end{figure}

L'intera fase di analisi e mappatura è documentata nel notebook \texttt{01\_\allowbreak{}Data\_\allowbreak{}Analysis\_\allowbreak{}and\_\allowbreak{}Mapping.ipynb}.

\subsection{Generazione Sintetica Avanzata}
\label{subsec:generazione_sintetica_depression}

Il cuore della preparazione dei dati per il modello di Depression Prediction è un \textbf{motore di generazione sintetica} appositamente progettato, documentato nel notebook \texttt{02\_\allowbreak{}Advanced\_\allowbreak{}Synthetic\_\allowbreak{}Generation.ipynb}. Il generatore produce \textbf{100.000 pazienti virtuali}, ciascuno dotato di una serie temporale di 14 giorni di check-in emotivi sulla scala SINTONIA e di un punteggio PHQ-9 associato.

\subsubsection{Filosofia: Nucleo Clinico e Rumore Empirico}

La logica di generazione segue un principio architetturale rigoroso: i \textbf{parametri fondamentali} sono definiti come costanti immutabili derivate dalla letteratura scientifica peer-reviewed, mentre i \textbf{dataset reali} contribuiscono esclusivamente varianza statistica (deviazioni standard) e matrici di transizione di Markov, con il ruolo limitato di conferire naturalezza e variabilità umana alle serie temporali generate.

Questa scelta è motivata dai limiti dei dataset di riferimento precedentemente analizzati: il campione ristretto e specifico dello StudentLife non è rappresentativo della popolazione generale, e la struttura invertita del 14-Day AA non rispecchia il flusso dati di SINTON-IA.

\subsubsection{Architettura a Tre Livelli}

Il generatore opera su tre livelli funzionali, ciascuno governato da parametri clinici rigorosamente referenziati:

\paragraph{Livello 1: Profilo Paziente e Parametri Emotivi.}
A ciascun paziente viene assegnata una fascia di severità PHQ-9, secondo la distribuzione epidemiologica documentata dall'Organizzazione Mondiale della Sanità \cite{who2023depression} e dagli studi italiani di De Girolamo et al. \cite{degirolamo2020mental}. Il punteggio PHQ-9 specifico all'interno della fascia è campionato da una distribuzione Beta(2,2) troncata, che evita artefatti ai bordi.

I parametri emotivi di ciascun paziente --- la valenza media attesa e la sua variabilità --- sono definiti dalla letteratura sull'\textbf{inerzia emotiva}: Kuppens et al. \cite{kuppens2010inertia} hanno dimostrato che i soggetti con sintomi depressivi presentano emozioni negative più persistenti e un tono affettivo medio significativamente più basso, mentre Houben et al. \cite{houben2015emotion} hanno confermato tramite meta-analisi la correlazione tra dinamiche emotive negative a breve termine e ridotto benessere psicologico. Le deviazioni standard empiriche estratte dai dataset reali (Notebook 01) vengono integrate con un peso del 30\% per modulare la varianza ``umana'' senza alterare i parametri di base.

\paragraph{Livello 2: Catena di Markov Emotiva.}
Le transizioni giornaliere tra stati emotivi non sono generate casualmente, ma seguono catene di Markov del primo ordine le cui matrici di transizione $P(X_t \mid X_{t-1})$ sono state estratte empiricamente dai dati reali nel Notebook 01. Queste matrici catturano l'\textbf{inerzia emotiva naturale}: la tendenza degli esseri umani a rimanere nello stesso stato d'animo da un giorno all'altro, un fenomeno particolarmente marcato nei soggetti depressi come documentato da Kuppens et al. \cite{kuppens2010inertia} e dal lavoro sul \textit{critical slowing down} di Wichers et al. \cite{wichers2016critical}.

\paragraph{Livello 3: Meccanismi di Rumore Clinico.}
Due fenomeni clinici documentati in letteratura vengono simulati per irrobustire il dataset:

\begin{itemize}
    \item \textbf{Dropout MNAR (Missing Not At Random):} Nella pratica del telemonitoraggio, i pazienti più gravi tendono ad abbandonare le sessioni di check-in con maggiore frequenza \cite{karyotaki2015dropout}. Mohr et al. \cite{mohr2017personal} confermano che l'aderenza al \textit{personal sensing} cala nei quadri di anedonia e letargia. Il generatore applica tassi di dropout crescenti con la severità (dal 5\% per la fascia minima al 40\% per la fascia severa), ulteriormente modulati dalla presenza di streak negative consecutive: più giorni consecutivi di umore negativo aumentano la probabilità di saltare il check-in successivo, come documentato da aan het Rot et al. \cite{aanhetrot2012ema}.

    \item \textbf{Smiling Depression:} Helmich et al. \cite{helmich2020smiling} hanno documentato pattern non lineari in cui pazienti con depressione severa mostrano apparenti miglioramenti temporanei. Per il 7--10\% dei pazienti nelle fasce moderate e severe, il generatore inietta un bias positivo nella valenza riportata: il paziente sembra ``stare bene'' nell'app, ma il suo PHQ-9 reale è elevato. Questo meccanismo previene il \textit{data leakage} e costringe il modello a non affidarsi esclusivamente all'umore medio dichiarato.
\end{itemize}

Un ulteriore fenomeno modellato è l'\textbf{appiattimento affettivo} (\textit{emotion context insensitivity}): Rottenberg et al. \cite{rottenberg2005mood} hanno dimostrato che i soggetti con depressione maggiore sperimentano emozioni meno intense sia in contesti positivi che negativi. Nel generatore, l'intensità emotiva riportata dai pazienti delle fasce più gravi viene attenuata tramite un coefficiente di smorzamento proporzionale alla severità.

\subsection{Validazione del Dataset Generato}
\label{subsec:validazione_sintetico}

Il dataset sintetico generato è stato sottoposto a una serie di verifiche per confermarne la plausibilità clinica e l'assenza di artefatti:

\begin{itemize}
    \item \textbf{Distribuzione della severità:} la distribuzione delle fasce PHQ-9 nel dataset generato riproduce fedelmente i target epidemiologici definiti dalla letteratura.
    \item \textbf{Distribuzione degli stati d'animo per fascia:} come atteso, i pazienti con severità maggiore mostrano una prevalenza di stati negativi (Triste, Ansioso, Stanco), mentre i pazienti sani esprimono prevalentemente stati positivi (Figura~\ref{fig:mood_dist}).
    \item \textbf{Missing data MNAR:} il tasso di dati mancanti cresce sistematicamente con la severità, confermando il corretto funzionamento del meccanismo MNAR.
    \item \textbf{Verifica anti-leakage:} la correlazione tra valenza media e punteggio PHQ-9 è stata verificata per assicurare che sia forte ma non perfetta (Figura~\ref{fig:leakage_check}), a conferma del fatto che i casi di smiling depression e il rumore clinico impediscono al modello di prendere scorciatoie predittive banali.
\end{itemize}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{dp_mood_distribution.png}
    \caption{Distribuzione percentuale degli stati d'animo SINTONIA per ciascuna fascia di severità PHQ-9 nel dataset sintetico generato.}
    \label{fig:mood_dist}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{dp_leakage_check.png}
    \caption{Scatter plot PHQ-9 vs valenza media per paziente. I punti rossi indicano i casi di \textit{smiling depression}: pazienti con PHQ-9 elevato ma valenza media positiva.}
    \label{fig:leakage_check}
\end{figure}

% ── 3.3 ──────────────────────────────────────────────────────────────────────
\clearpage
\section{Dati per il Churn Prevention}
\label{sec:data_churn}

\subsection{Natura del Problema e Approccio ai Dati}
\label{subsec:natura_churn}

Il Churn Prevention si distingue radicalmente dai due componenti precedenti per la natura stessa del task affrontato. Mentre il Red Flag Detection e il Depression Prediction sono rispettivamente problemi di classificazione e regressione --- paradigmi di \textit{apprendimento supervisionato} che necessitano di dataset esterni etichettati per l'addestramento --- il sistema di prevenzione dell'abbandono è formulato come un \textbf{problema di ottimizzazione} risolto mediante un Algoritmo Genetico (GA).

Questa differenza fondamentale comporta tre conseguenze dirette sull'approccio ai dati:
\begin{enumerate}
    \item \textbf{Nessun dataset esterno:} il GA non ``impara'' da esempi storici etichettati. Il suo compito è \textit{evolvere} strategie di \textit{nudging} (notifiche personalizzate) che massimizzino la probabilità di mantenere attivo l'utente sulla piattaforma. Non è quindi necessario reperire dataset pubblici o accademici da fonti terze.
    \item \textbf{Dati interni come sorgente esclusiva:} l'algoritmo si alimenta esclusivamente dei dati comportamentali generati dall'interazione dei pazienti con la piattaforma SINTONIA.
    \item \textbf{Nessuna etichettatura necessaria:} a differenza del ML supervisionato, il GA non richiede label. La qualità delle soluzioni candidate viene valutata da una \textbf{funzione di fitness} che utilizza gli indicatori comportamentali dell'utente come input per calcolare un punteggio di bontà della strategia proposta.
\end{enumerate}

Di conseguenza, la \textit{Data Understanding} per questo modello non consiste nell'esplorare un dataset scaricato, bensì nel \textbf{mappare sistematicamente quali dati il backend di SINTONIA produce e persiste}, valutandone l'idoneità come input per la funzione di fitness dell'Algoritmo Genetico.

\subsection{Inventario degli Indicatori Comportamentali}
\label{subsec:inventario_indicatori}

L'analisi dello schema del database relazionale di SINTONIA (implementato tramite Drizzle ORM su PostgreSQL) ha permesso di identificare \textbf{sette entità} le cui informazioni risultano direttamente rilevanti per la costruzione della funzione di fitness del GA. Per ciascuna entità vengono riportati i campi significativi e l'indicatore comportamentale derivabile.

\subsubsection{Stato d'Animo (\texttt{stato\_animo})}

La tabella \texttt{stato\_animo} registra ogni inserimento dello stato emotivo da parte del paziente. I campi rilevanti sono: \texttt{umore} (un valore categorico tra 10 stati predefiniti: Felice, Sereno, Energico, Neutro, Stanco, Triste, Ansioso, Arrabbiato, Spaventato, Confuso), \texttt{intensita} (valore intero) e \texttt{data\_inserimento} (timestamp).

Questa tabella costituisce la fonte primaria di indicatori per il GA. Dalla sequenza temporale degli inserimenti è possibile derivare:
\begin{itemize}
    \item La \textbf{frequenza di registrazione del mood} in una finestra temporale (es. inserimenti negli ultimi 7 giorni), direttamente proporzionale al livello di engagement dell'utente.
    \item La \textbf{regolarità} della compilazione (streak), calcolata come percentuale di giorni con almeno un inserimento sul totale dei giorni della finestra di osservazione. Il sistema di Streak già implementato nella piattaforma si basa esattamente su questo dato.
    \item Il \textbf{trend della valenza emotiva}: una predominanza persistente di umori a valenza negativa (Triste, Ansioso, Spaventato) potrebbe correlare con un aumento del rischio di abbandono.
\end{itemize}

\subsubsection{Pagine del Diario (\texttt{pagina\_diario})}

La tabella \texttt{pagina\_diario} memorizza le pagine scritte nel diario personale, con i campi \texttt{titolo}, \texttt{testo} e \texttt{data\_inserimento}.

Gli indicatori derivabili sono:
\begin{itemize}
    \item La \textbf{frequenza di scrittura} nel diario, indicativa dell'interesse del paziente verso lo strumento terapeutico.
    \item La \textbf{lunghezza media dei testi} (in caratteri), che permette di distinguere compilazioni genuine e riflessive da inserimenti superficiali o frettolosi.
    \item I \textbf{giorni trascorsi dall'ultima pagina scritta}, un potente segnale di disengagement quando il valore è elevato.
\end{itemize}

\subsubsection{Questionari (\texttt{questionario})}

La tabella \texttt{questionario} traccia le compilazioni dei questionari clinici, includendo \texttt{score}, \texttt{data\_compilazione}, i flag \texttt{revisionato} e \texttt{invalidato}.

Da questa entità si ricavano:
\begin{itemize}
    \item Il \textbf{tasso di compliance}: rapporto tra questionari effettivamente completati e questionari previsti dal calendario di somministrazione. Un calo della compliance è un indicatore precoce di disengagement.
    \item Il \textbf{tasso di invalidazione}: percentuale di compilazioni successivamente invalidate dallo psicologo, potenziale segnale di \textit{careless responding} tipico di utenti che stanno perdendo interesse.
\end{itemize}

\subsubsection{Forum (\texttt{domanda\_forum})}

La tabella \texttt{domanda\_forum} registra le domande pubblicate dal paziente nel forum comunitario, con \texttt{titolo}, \texttt{testo}, \texttt{categoria} e \texttt{data\_inserimento}.

L'indicatore derivabile è il livello di \textbf{engagement sociale}: un paziente che interagisce attivamente nel forum è presumibilmente più coinvolto nell'ecosistema SINTONIA rispetto a uno che non vi partecipa.

\subsubsection{Notifiche (\texttt{notifica})}

La tabella \texttt{notifica} è di particolare importanza strategica per il GA. Oltre ai campi \texttt{titolo}, \texttt{tipologia}, \texttt{descrizione} e \texttt{data\_invio}, essa include il campo \texttt{letto} (boolean): un flag che indica se l'utente ha effettivamente visualizzato la notifica.

Questo campo costituisce il \textbf{meccanismo di feedback diretto} per l'Algoritmo Genetico: consente di valutare se le notifiche generate da una strategia di \textit{nudging} vengono effettivamente lette o sistematicamente ignorate dall'utente. Il \textbf{tasso di lettura} delle notifiche (rapporto \texttt{letto = true} / totale inviate) diventa così un componente fondamentale della funzione di fitness.

\subsubsection{Paziente (\texttt{paziente})}

La tabella anagrafica \texttt{paziente} contiene il campo \texttt{data\_ingresso} (data di inserimento nella lista d'attesa), dal quale si calcola il numero di \textbf{giorni in lista d'attesa}. Questo indicatore è cruciale poiché la letteratura evidenzia una correlazione diretta tra la durata dell'attesa e la probabilità di abbandono \cite{baumel2019mhealth}. Risultano inoltre rilevanti il campo \texttt{id\_priorita} (livello di priorità nel triage) e \texttt{id\_psicologo} (che indica se il paziente è già stato preso in carico).

\subsubsection{Badge (\texttt{acquisizione\_badge})}

La tabella \texttt{acquisizione\_badge} traccia i traguardi di gamification raggiunti dal paziente, con \texttt{nome\_badge} e \texttt{data\_acquisizione}. Il numero totale di badge ottenuti e la loro distribuzione temporale forniscono un indicatore dell'\textbf{engagement con il sistema di gamification} integrato nella piattaforma.

\subsection{Dati Non Attualmente Tracciati}
\label{subsec:dati_mancanti_churn}

L'analisi dello schema ha evidenziato l'assenza di due categorie di dati potenzialmente utili:

\begin{itemize}
    \item \textbf{Sessioni di accesso:} il backend gestisce l'autenticazione tramite JWT e integrazione SPID, ma non persiste in una tabella dedicata i timestamp di login/logout degli utenti. Ne consegue l'impossibilità di calcolare direttamente la frequenza di apertura dell'applicazione e la durata media delle sessioni.
    \item \textbf{Durata delle sessioni:} non essendo tracciato il momento di inizio e fine di ciascuna sessione, non è possibile distinguere accessi brevi (apertura accidentale o consultazione rapida) da sessioni di utilizzo prolungato e significativo.
\end{itemize}

Questa lacuna, tuttavia, \textbf{non risulta bloccante} per il funzionamento dell'Algoritmo Genetico. Il pattern di utilizzo dell'utente è ricostruibile indirettamente con buona approssimazione analizzando i timestamp (\texttt{data\_inserimento}) presenti nelle tabelle \texttt{stato\_animo}, \texttt{pagina\_diario}, \texttt{questionario} e \texttt{domanda\_forum}: ogni azione registrata implica necessariamente un accesso alla piattaforma in quel momento. Pertanto, la distribuzione temporale delle azioni funge da \textit{proxy} affidabile della frequenza e del pattern orario di utilizzo.
